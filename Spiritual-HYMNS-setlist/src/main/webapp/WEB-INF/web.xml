<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Worship Song Library</display-name>
    <description>Church worship song library with chord transposition and leaflet builder</description>

    <!-- Character Encoding Filter — applies UTF-8 to all requests for multi-language support -->
    <filter>
        <filter-name>CharacterEncodingFilter</filter-name>
        <filter-class>com.worship.util.CharacterEncodingFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>CharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Welcome File — landing page -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Session timeout: 30 minutes (reduced from 60 for security) -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

</web-app>
